
function multiplicacao(num1: number, num2: number): number {
    return num1 * num2;
}

function saudacao(nome: string): string {
    return "Olá " + nome;
}

console.log(multiplicacao(4, 5)); // Saída: 20
console.log(saudacao("Kaique")); // Saída: Olá Kaique
